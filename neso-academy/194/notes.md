# Structures and Functions (Part 1)

We can pass struct members using dot notation to functions.
we just need to know the type of that member.
We can also pass those members by reference using the address of operator (`&`)
