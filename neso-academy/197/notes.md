# Structures and Functions (Part 4)

When passing a variable that implements a structure by reference we can access it's members using the
little arrrow notation (`->`):

We can return those variables just like normal variables, just need to make sure that the return type
of that function matches the type of the variable.
